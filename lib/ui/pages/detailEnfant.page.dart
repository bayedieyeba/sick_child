import 'package:flutter/material.dart';

class DetailEnfant extends StatelessWidget {
  const DetailEnfant({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle: true,
        title: const Text("Bara Fall"),
      ),
      body: Container(),
    );
  }
}
